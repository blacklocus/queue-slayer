sourceSets {
    main {
        resources {
            srcDir 'src/main/elasticsearch'
        }
    }
}

dependencies {
    compile project(':qs-worker-core')

    compile deps.jres

    testCompile deps.jres_test
}